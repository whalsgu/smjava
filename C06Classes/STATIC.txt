[static]

1. 클래스의 정적멤버는 하나의 클래스로부터 생성된 객체들 가운데 하나만 생성된다.
2. 클래스를 생성(new)하지 않고 사용할 수 있다.
   - 클래스가 객체화되지 않아도 사용할 수 있다.
   - 클래스 변수, 클래스 멤버
   - this가 없다.   
3. 클래스 로드되면 사용할 수 있다.   

4. 정적멤버
   - 정적 속성
   - 정적 메소드
   - 정적 상수
   
5. static은 객체별로 생성하지 않고 공용으로 사용한다.   
   
   


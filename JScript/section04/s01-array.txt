배열(array)

(자바스크립트 배열의 특징)
1. 배열 리터럴 : 대괄호([])
2. 크기를 지정하지 않아도 된다.
3. 인덱스를 통해서 접근: 0부터 시작
4. 동적으로 배열의 원소를 추가할 수 있다.
5. 배열의 크기는 배열의 인덱스에서 가장 큰 값을 기준으로 정해지며 크기는 (인덱스 + 1)
6. 값이 지정되지 않는 인덱스의 요소는 undefined
7. 동일한 자료형이 아닌 여러 자료형을 요소에 넣을 수 있다.
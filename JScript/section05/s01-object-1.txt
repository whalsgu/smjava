자바스크립트 객체(Object) 기반 프로그래밍 언어

1. 속성(Property)
2. 메소드(Method)

--------------------------------------------------------------------
(객체 생성자 함수 정의)
function 함수명(매개변수1, 매개변수2, ... 매개변수n) {
    this.속성명 = 값;

    this.함수명 = function() { // 메소드
        ...
    }
}

var 참조변수 = new 객체생성자함수();
참조변수.속성명 = 값;
var 변수 = 참조변수.속성명;
참조변수.함수명();


--------------------------------------------------------------------
(컴포넌트 기반)
1. 속성
2. 메소드
3. 이벤트
4. 언어: Delphi(델파이), C++ Builder